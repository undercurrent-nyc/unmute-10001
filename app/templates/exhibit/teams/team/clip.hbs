<Clip @clip={{@model}}>
<div class="pt-4 flex flex-row w-full justify-between text-sm md:text-base font-bold">
  <h2>
    “{{@model.title}}”
  </h2>
  <p>{{format-date @model.postDate}}</p>
</div>
</Clip>
